// src/lib/firebase.ts

// ====================================================================
// 로컬 목업 모드 활성화됨
//
// 현재 클라우드 프로젝트 설정 문제로 인해 Firebase 와의 모든 통신을 비활성화하고,
// 앱이 100% 로컬 가짜 데이터로만 작동하도록 전환되었습니다.
//
// 나중에 클라우드 문제가 해결되면 이 파일의 내용을 복원해야 합니다.
// ====================================================================

export const firebaseConfig = {};

// 목업(가짜) 객체를 내보내서 앱의 다른 부분에서 오류가 발생하지 않도록 합니다.
const db = {};
const auth = {};
const storage = {};
const app = {};

export { db, auth, storage, app };
