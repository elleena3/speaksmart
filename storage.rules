rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // For this sample, we allow all reads and writes.
    // In a real application, you should secure your data.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
